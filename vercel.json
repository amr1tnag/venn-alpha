{
  "buildCommand": "npx expo export --platform web && node scripts/inject-html-meta.js && cp signup.html dist/signup.html",
  "outputDirectory": "dist",
  "routes": [
    { "src": "^/$", "dest": "/signup.html" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
